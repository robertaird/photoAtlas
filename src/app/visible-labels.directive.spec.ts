import { VisibleLabelsDirective } from './visible-labels.directive';

describe('VisibleLabelsDirective', () => {
  it('should create an instance', () => {
    const directive = new VisibleLabelsDirective();
    expect(directive).toBeTruthy();
  });
});
